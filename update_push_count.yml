name: Update Push Count

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  update-push-count:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14

    - name: Install dependencies
      run: npm install

    - name: Update Push Count
      run: node update_push_count.js
      env:
        GITHUB_TOKEN: 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
        README_PATH: 'README.md'
